<html>
    include header.pug
    
    <body>

        div(class="container text-center")
            form(class="ui form" action='/mail' method="post" enctype="multipart/form-data")
                h2= "Mail"
                 div(class="field")
                    <br/>
                    label(for="email")= "E-Mail"
                    input(type="email" placeholder="email" id="email" name="email")
                    label(for="token")= "Token"
                    input(type="text" placeholder="Token" id="token" name="token")
                    <br/><br/>
                    input(type="file" id="pdf" name="pdf" multiple="multiple")
                button(class="btn" id="mail_bottom" style="background-color:blue;color: white;margin-bottom:1vh")= "Send Mail"
                hr

        <script src="../pathology.js"></script>